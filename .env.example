# Optional overrides for the backend API and messaging service URLs.
# If unset, the app falls back to the production Husridge services
# (husridge-server and messaging-chat on Render) — safe to leave unset.
VITE_API_BASE_URL=
VITE_MESSAGING_BASE_URL=
